import 'package:flutter/material.dart';

class MasterLaundryScreen extends StatelessWidget {
  const MasterLaundryScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: Column(children: <Widget>[]),
    );
  }
}
